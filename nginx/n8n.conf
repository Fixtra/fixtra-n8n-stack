server {
    listen 443 ssl http2;  # Dodany parametr ssl
    server_name wizardengine.fixtra.io;
    
    # Certyfikaty SSL
    ssl_certificate /etc/letsencrypt/live/wizardengine.fixtra.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/wizardengine.fixtra.io/privkey.pem;
    
    # Nagłówki bezpieczeństwa
    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options SAMEORIGIN;
    add_header X-XSS-Protection "1; mode=block";
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; connect-src 'self' wss://$host; img-src 'self' data:; style-src 'self' 'unsafe-inline'; frame-src 'self' *;";
    add_header Referrer-Policy "no-referrer-when-downgrade";
    
    # Limit rozmiaru zapytań
    client_max_body_size 16M;
    
    # Konfiguracja timeoutów
    proxy_connect_timeout 600;
    proxy_send_timeout 600;
    proxy_read_timeout 600;
    send_timeout 600;
    
    # n8n - główna ścieżka
    location / {
        proxy_pass http://127.0.0.1:5678;
        
        # Podstawowe nagłówki proxy
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Wsparcie dla WebSocketów
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Wyłączenie buforowania
        proxy_buffering off;
    }
    
    # Langfuse - aplikacja Next.js
    location /langfuse/ {
        proxy_pass http://127.0.0.1:3000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Base "/langfuse";
        
        # Zwiększenie timeoutów
        proxy_connect_timeout 300s;
        proxy_read_timeout 300s;
        
        # Buforowanie
        proxy_buffer_size 128k;
        proxy_buffers 4 256k;
        proxy_busy_buffers_size 256k;
    }

    # Obsługa API Langfuse
    location /langfuse/api/ {
        proxy_pass http://127.0.0.1:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Obsługa statycznych plików Langfuse
    location ~ ^/langfuse/_next/(.*)$ {
        proxy_pass http://127.0.0.1:3000/_next/$1;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Buforowanie statycznych plików
        expires 30d;
        add_header Cache-Control "public, max-age=2592000";
    }
    
    # Logi
    access_log /var/log/nginx/wizardengine.access.log;
    error_log /var/log/nginx/wizardengine.error.log warn;
}

server {
    listen 80;
    server_name wizardengine.fixtra.io;
    return 301 https://$host$request_uri;
}
